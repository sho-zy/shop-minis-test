import {theme as defaultTheme, Theme} from '@shopify/shop-minis-platform-sdk'

import {QuickstartNavigator} from './routes'

export const theme: Theme = {
  ...defaultTheme,
}

export default QuickstartNavigator
